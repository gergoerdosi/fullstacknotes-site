---
title: Overview
description: A collection of practical notes and tips for modern web development.
template: doc
tableOfContents: false
hero:
  title: import { knowledge }
  tagline: A collection of practical notes and tips for modern web development.
---

import WordCloud from '../../components/WordCloud.astro';

<WordCloud />

<style is:global>{`
  /* 1. Page Layout & Content Width */
  :root {
    --sl-content-width: 60rem;
  }

  /* 2. Force Hero Centering */
  .hero {
    grid-template-columns: 1fr !important;
    justify-items: center !important;
    text-align: center !important;
    padding-bottom: 4rem;
  }

  .hero .stack {
    align-items: center !important;
    text-align: center !important;
    margin-inline: auto !important;
    max-width: 800px;
  }

  /* 3. Hero Title & Tagline */
  .hero h1 {
    width: 100%; 
    position: relative;
    display: inline-block;
    z-index: 1;
  }

  .hero .tagline {
    margin-inline: auto;
    font-size: 1.6rem;
    line-height: 1.6;
    color: var(--sl-color-gray-2);
  }

  /* 4. Glow Effect (Dark Mode Only) */
  :root[data-theme='dark'] .hero h1::before {
    content: "";
    position: absolute;
    z-index: -1;
    
    /* Center positioning */
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    
    /* Visuals */
    width: 600px;
    height: 600px;
    border-radius: 50%;
    
    background: radial-gradient(
      closest-side, 
      var(--sl-color-accent-low), 
      transparent 100%
    );
    filter: blur(70px);
    opacity: 0.4;
    pointer-events: none;
  }

  footer {
    display: none;
  }
`}</style>