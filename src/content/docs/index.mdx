---
title: Overview
description: A collection of practical notes and tips for modern web development.
template: doc
tableOfContents: false
hero:
  title: import { knowledge }
  tagline: A collection of practical notes and tips for modern web development.
---

import TechChips from '../../components/TechChips.astro';

<TechChips />

<style is:global>{`
  /* Page Layout & Content Width */
  :root {
    --sl-content-width: 60rem;
  }

  /* Force Hero Centering */
  .hero {
    grid-template-columns: 1fr !important;
    justify-items: center !important;
    text-align: center !important;
    padding-bottom: 4rem;
    max-width: 100%;
    overflow-x: clip; 
  }

  /* --- MOBILE SPECIFIC TWEAKS --- */
  @media (max-width: 50rem) {
    /* Add top spacing on mobile */
    .hero {
      padding-top: 4rem; 
    }
    
    /* Reduce tagline size on mobile */
    .hero .tagline {
      font-size: 1.1rem !important; /* Reduced from 1.6rem */
      line-height: 1.4 !important;
    }
  }

  .hero .stack {
    align-items: center !important;
    text-align: center !important;
    margin-inline: auto !important;
    max-width: 800px;
    width: 100%; 
  }

  /* Hero Title & Tagline */
  .hero h1 {
    width: 100%; 
    position: relative;
    display: inline-block;
    z-index: 1;
  }

  .hero .tagline {
    margin-inline: auto;
    font-size: 1.6rem;
    line-height: 1.6;
    color: var(--sl-color-gray-2);
  }

  /* Glow Effect (Dark Mode Only) */
  :root[data-theme='dark'] .hero h1::before {
    content: "";
    position: absolute;
    z-index: -1;
    
    /* Center positioning */
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 100%;       /* Use 100% of the parent... */
    max-width: 600px;  /* ...up to a maximum of 600px */
    aspect-ratio: 1/1; /* Keeps it a perfect circle/square without fixed height */
    
    border-radius: 50%;
    
    background: radial-gradient(
      closest-side, 
      var(--sl-color-accent), 
      transparent 100%
    );
    
    filter: blur(80px);
    opacity: 0.2; 
    pointer-events: none;
  }

  /* Hide Default Footer */
  footer {
    display: none;
  }
`}</style>